# Copy to LocalSecrets.xcconfig (gitignored). In Xcode:
# Project → Info → Configurations → Debug / Release → WCS-Platform row →
# "Based on Configuration File" → choose LocalSecrets.xcconfig (repo root).
#
# YOUTUBE_DATA_API_KEY = AIza_your_youtube_data_api_v3_key
